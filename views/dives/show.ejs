<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= dive.name %></h1>
    <div>
      <li>Depth: <%= dive.depth %></li>
      <li>Time: <%= dive.time %></li>
      <li>Weight?: <%= dive.weight %></li>
      <li>Description: <%= dive.description %></li>
    </div>
    <% if (String(dive.diver) === String(user._id)) { %>
    <a href="/dives/<%= dive._id %>/edit"><button>Edit</button></a>
    <form action="/dives/<%= dive._id %>?_method=DELETE" method="POST">
      <button type="submit">Delete this dive</button>
    </form>
    <% } %>
  </body>
</html>

